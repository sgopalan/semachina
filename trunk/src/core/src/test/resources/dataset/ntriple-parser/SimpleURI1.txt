ClauseQuery(
  UnaryClause(
    SimpleExpression(
      TriplePattern(
        Wildcard(
          *
        ) [Wildcard]
        Wildcard(
          *
        ) [Wildcard]
        URI(
          s
        ) [URI]
      ) [TriplePattern]
    ) [SimpleExpression]
  ) [UnaryClause]
) [ClauseQuery]
