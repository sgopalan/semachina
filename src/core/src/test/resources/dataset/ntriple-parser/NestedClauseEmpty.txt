ClauseQuery(
  BinaryClause(
    SimpleExpression(
      TriplePattern(
        URI(
          s
        ) [URI]
        URI(
          p
        ) [URI]
        URI(
          obj
        ) [URI]
      ) [TriplePattern]
    ) [SimpleExpression]
    0
    QueryExpression(
      EmptyQuery(
      ) [EmptyQuery]
    ) [QueryExpression]
  ) [BinaryClause]
) [ClauseQuery]
