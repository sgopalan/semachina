ClauseQuery(
  UnaryClause(
    SimpleExpression(
      TriplePattern(
        Wildcard(
          *
        ) [Wildcard]
        URI(
          http://ns/#s
        ) [URI]
        URI(
          http://ns/p
        ) [URI]
      ) [TriplePattern]
    ) [SimpleExpression]
  ) [UnaryClause]
) [ClauseQuery]
